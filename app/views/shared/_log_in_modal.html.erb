			<!-- Iniciar sessió Modal -->
<div class="modal fade" id="login" tabindex="-1" role="dialog" aria-labelledby="registermodal" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered login-pop-form" role="document">
    <div class="modal-content" id="registermodal">
      <span class="mod-close" data-bs-dismiss="modal" aria-hidden="true"><i class="fas fa-times"></i></span>
      <div class="modal-body">
        <h4 class="modal-header-title">Iniciar sessió</h4>
        <div class="login-form">
          <%# test %>
          <h2>Iniciar sessió</h2>
          <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name), data: { turbo: :false }) do |f| %>
            <div class="form-inputs">
              <%= f.input :email,
                required: false,
                autofocus: true,
                input_html: { autocomplete: "email" } %>
              <%= f.input :password,
                required: false,
                input_html: { autocomplete: "current-password" } %>
              <%= f.input :remember_me, as: :boolean if devise_mapping.rememberable? %>
            </div>
            <div class="form-actions">
              <%= f.button :submit, "Entrar a Sistach Finques", class: "btn btn-md full-width btn-theme-light-2 rounded" %>
            </div>
            <div class="text-center">
              <p class="mt-2"><%= link_to "No recordes la contrasenya?", new_password_path(resource_name), class: "link" %></p>
            </div>
          <% end %>
          <div class="modal-divider"><span>O</span></div>
          <div>
            <%= link_to "Apunta't", new_registration_path(resource_name), class: "btn btn-md full-width btn-secondary rounded" %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
<!-- End Modal -->
